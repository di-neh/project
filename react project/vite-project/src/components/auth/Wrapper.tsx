import styled from "styled-components";

const Wrapperr = styled.div`
  background: #2F3136;
`

const Wrapper = () => {
    return (
        <Wrapperr>

        </Wrapperr>
    );
};

export default Wrapper;